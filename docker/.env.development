# 🔧 UrbanSend - Configuração de Desenvolvimento Local

# === CONFIGURAÇÕES GERAIS ===
NODE_ENV=development
PORT=3001
PUBLIC_URL=http://localhost:3010

# === CONFIGURAÇÕES DE DOMÍNIO ===
DOMAIN=localhost
FRONTEND_URL=http://localhost:3010
API_URL=http://localhost:3010/api

# === CONFIGURAÇÕES DO BANCO DE DADOS ===
DATABASE_URL=/app/data/database.sqlite

# === CONFIGURAÇÕES DE SEGURANÇA ===
JWT_SECRET=dev_jwt_secret_key_32_characters_long_minimum
COOKIE_SECRET=dev_cookie_secret_32_characters_long
API_KEY_SALT=dev_api_key_salt_32_characters_long

# === CONFIGURAÇÕES CORS ===
ALLOWED_ORIGINS=http://localhost:3010,http://localhost:5173,http://localhost:3000

# === CONFIGURAÇÕES SMTP ===
SMTP_SERVER_PORT=25
SMTP_HOSTNAME=localhost
SMTP_MAX_CLIENTS=10

# === CONFIGURAÇÕES DE EMAIL ===
FROM_EMAIL=noreply@localhost
FROM_NAME=UrbanSend Dev

# === CONFIGURAÇÕES DE LOGS ===
LOG_LEVEL=debug
LOG_FILE=/app/logs/app-dev.log

# === CONFIGURAÇÕES DE RATE LIMITING (RELAXADAS) ===
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# === CONFIGURAÇÕES DE UPLOAD ===
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/app/data/uploads