# Production Environment Configuration
# IMPORTANT: Do not commit this file to version control
# Copy and customize for your production environment

NODE_ENV=production

# Server Configuration
PORT=3001
HTTPS_PORT=443
HOST=0.0.0.0

# Database Configuration (REQUIRED)
DATABASE_URL=sqlite:./ultrazend.sqlite

# Security Configuration (REQUIRED)
# Generate strong secrets with: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-super-secret-refresh-key-at-least-32-characters-long
JWT_REFRESH_EXPIRES_IN=30d
COOKIE_SECRET=your-super-secret-cookie-key-at-least-32-characters-long

# Rate Limiting Configuration (Flexibilizado para testes em VPS)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
RATE_LIMIT_SKIP_SUCCESSFUL_REQUESTS=false

# CORS Configuration
ALLOWED_ORIGINS=https://ultrazend.com.br,https://www.ultrazend.com.br
FRONTEND_URL=https://www.ultrazend.com.br
API_BASE_URL=https://www.ultrazend.com.br/api

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM_NAME=Your Company Name
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_HOSTNAME=mail.yourdomain.com
SMTP_SERVER_PORT=25

# Logging Configuration (Production)
LOG_LEVEL=warn
LOG_FILE_PATH=/var/log/urbansend/app.log
LOG_MAX_SIZE=20m
LOG_MAX_FILES=5

# Security Headers (Production)
HSTS_MAX_AGE=31536000
CSP_REPORT_ONLY=false

# Performance Configuration
BCRYPT_SALT_ROUNDS=12

# Feature Flags (Production)
ENABLE_SWAGGER=false
ENABLE_METRICS=true

# External Services
REDIS_URL=redis://localhost:6379
WEBHOOK_SECRET=your-webhook-secret-key

# SSL Configuration (if using HTTPS)
# SSL_KEY_PATH=/path/to/your/private.key
# SSL_CERT_PATH=/path/to/your/certificate.crt

# Trust Proxy Configuration (for load balancers/reverse proxies)
TRUST_PROXY=1

# Health Check Configuration
HEALTH_CHECK_TIMEOUT=5000
HEALTH_CHECK_INTERVAL=30000