# =============================================================================
# ULTRAZEND ENTERPRISE CONFIGURATION
# Complete environment configuration for production-ready deployment
# =============================================================================

# Server Configuration
NODE_ENV=development
PORT=3001
HTTPS_PORT=443
HOST=0.0.0.0

# Database Configuration
DB_CLIENT=pg
DATABASE_URL=postgresql://ultrazend:ultrazend@localhost:5432/ultrazend?schema=public
# SQLite fallback (legacy):
# DB_CLIENT=sqlite
# DATABASE_URL=./ultrazend.sqlite

# Security Configuration
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-jwt-refresh-secret-minimum-32-characters
JWT_REFRESH_EXPIRES_IN=30d
COOKIE_SECRET=your-cookie-secret-minimum-32-characters

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_SKIP_SUCCESSFUL_REQUESTS=false

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
FRONTEND_URL=http://localhost:5173
API_BASE_URL=http://localhost:3001

# 🚀 ULTRAZEND SMTP CONFIGURATION (PURE MODE - NO POSTFIX)
ULTRAZEND_SMTP_HOST=mail.ultrazend.com.br
ULTRAZEND_SMTP_PORT=25                    # Porta principal UltraZend
ULTRAZEND_HOSTNAME=mail.ultrazend.com.br  
ULTRAZEND_DOMAIN=ultrazend.com.br

# SMTP Server Ports
SMTP_MX_PORT=2525           # SMTPServer interno (não conflita com Postfix)
SMTP_SUBMISSION_PORT=587    # Submission port (autenticado)
SMTP_USERNAME=noreply@ultrazend.com.br

# ❌ REMOVIDO: Configurações SMTP externo (Postfix/SendGrid)
# SMTP_HOST=localhost         # REMOVIDO - UltraZend usa entrega direta
# SMTP_PORT=587              # REMOVIDO - UltraZend usa entrega direta
# SMTP_SECURE=false          # REMOVIDO - UltraZend usa entrega direta
# SMTP_USER=                 # REMOVIDO - UltraZend usa entrega direta
# SMTP_PASS=                 # REMOVIDO - UltraZend usa entrega direta

# DKIM Configuration
DKIM_PRIVATE_KEY_PATH=./keys/dkim-private.pem
DKIM_SELECTOR=default
DKIM_DOMAIN=yourdomain.com

# Redis Configuration (for Bull Queue)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Email Analytics
TRACKING_DOMAIN=track.yourdomain.com
WEBHOOK_SECRET=your-webhook-secret-key

# File Upload
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,text/html,text/plain

# Logging Configuration
LOG_LEVEL=info
LOG_FILE_PATH=./logs/app.log
LOG_MAX_SIZE=20m
LOG_MAX_FILES=5

# Security Headers
HSTS_MAX_AGE=31536000
CSP_REPORT_ONLY=false

# Performance Configuration
BCRYPT_SALT_ROUNDS=12

# Feature Flags
ENABLE_SWAGGER=true
ENABLE_METRICS=true
ENABLE_HEALTH_CHECKS=true
ENABLE_DETAILED_HEALTH_CHECKS=true
ENABLE_SECURITY_HEADERS=true
ENABLE_REQUEST_LOGGING=true
ENABLE_PERFORMANCE_MONITORING=true
ENABLE_CORRELATION_IDS=true

# Health Check Configuration
HEALTH_CHECK_TIMEOUT=5000
HEALTH_CHECK_INTERVAL=30000

# Trust Proxy Configuration
TRUST_PROXY=1

# Development Overrides (only in development)
# DEV_DISABLE_RATE_LIMITING=false
# DEV_DETAILED_ERRORS=true

# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES (Override in .env.production)
# =============================================================================
# NODE_ENV=production
# PORT=3001
# HTTPS_PORT=443
# DATABASE_URL=/var/www/ultrazend/backend/ultrazend.sqlite
# FRONTEND_URL=https://www.ultrazend.com.br
# API_BASE_URL=https://www.ultrazend.com.br
# ALLOWED_ORIGINS=https://ultrazend.com.br,https://www.ultrazend.com.br
# LOG_FILE_PATH=/var/www/ultrazend/logs/app.log
# LOG_LEVEL=info
# RATE_LIMIT_MAX_REQUESTS=100
# BCRYPT_SALT_ROUNDS=12
